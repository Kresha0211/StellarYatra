@* @model ExamSubmissionViewModel

@{
    ViewData["Title"] = "Exam Completed";
}

<div class="container text-center mt-5">
    <div class="card shadow-lg p-4 mb-5 bg-white rounded" style="max-width: 600px; margin: 0 auto;">
        <div class="card-header bg-success text-white">
            <h2><i class="fas fa-check-circle"></i> Exam Completed</h2>
        </div>
        <div class="card-body">
            <h4 class="card-title">@Model.CourseName</h4>
            <hr />

            <div class="row mb-4">
                <div class="col-md-6">
                    <h5>Your Score</h5>
                    <h3 class="text-primary">@Model.Score</h3>
                </div>
                <div class="col-md-6">
                    <h5>Total Marks</h5>
                    <h3 class="text-info">@Model.TotalMarks</h3>
                </div>
            </div>

            <div class="progress mb-4" style="height: 30px;">
                <div class="progress-bar bg-success" role="progressbar"
                     style="width: @Model.Percentage%"
                     aria-valuenow="@Model.Percentage"
                     aria-valuemin="0"
                     aria-valuemax="100">
                    @Model.Percentage.ToString("0.00")%
                </div>
            </div>

            <p class="text-muted">
                <i class="fas fa-clock"></i> Time Taken: @Model.TimeTaken.ToString(@"mm\:ss")
            </p>
           @*  <p class="text-muted">
                <i class="fas fa-calendar-alt"></i> Completed on: @Model.CompletedAt.ToString("f")
            </p> *@
        @* </div>
        <div class="card-footer">
            <a asp-controller="Course" asp-action="Details" asp-route-id="@ViewBag.CourseId"
               class="btn btn-primary mr-2">
                <i class="fas fa-book"></i> Back to Course
            </a>
            <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">
                <i class="fas fa-home"></i> Go to Home
            </a>
        </div>
    </div>
</div> *@ 